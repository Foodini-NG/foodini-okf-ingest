# Modified by Foodini 2026-08-22: removed the R / Rust / C++ sections along with
# their bindings, and the src/*.o|so|dll rules that targeted R's src/ directory
# (this fork uses src/ for the Python package). See NOTICE.

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# uv
.uv/

# Catalogs / scratch
*.duckdb
*.duckdb.wal
*.sqlite
/_state/

# Rendered HTML output (the package ships the renderer, never rendered content).
# `okf-ingest html` emits .html — never commit it. The repo has no .html source.
*.html
site/
*_html/
_render/

# Built package artifacts
*.tar.gz
*.whl

# OS
.DS_Store
Thumbs.db
